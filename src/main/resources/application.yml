server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://bankingdbserver-mysql.mysql.database.azure.com:3306/account_db?serverTimezone=UTC
    username: bankingdbadmin
    password: Admin@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
